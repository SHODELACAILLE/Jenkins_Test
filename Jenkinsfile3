node {
  stage('SCM') {
    checkout scm
  }
  stage('SonarQube Analysis') {
    def mvn = tool 'Default Maven';
    withSonarQubeEnv() {
      sh "./mvnw clean verify sonar:sonar -Dsonar.projectKey=com.example:demo -Dsonar.projectName='demo'"
    }
  }
}